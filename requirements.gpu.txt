# GPU Production Dependencies (RunPod A100 80GB)
# Install with: pip install -r requirements.gpu.txt

# ---- Core Framework ----
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.40.0
twilio>=9.10.1
pydub>=0.25.1
soundfile>=0.12.1
numpy>=1.26.0
librosa>=0.11.0
resampy>=0.4.0
pydantic>=2.10.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
websockets>=16.0

# ---- PyTorch (CUDA 12.1) ----
torch>=2.4.0
torchaudio>=2.4.0

# ---- Speech-to-Text (STT) ----
faster-whisper>=1.2.0
# whisper_streaming is vendored at src/stt/whisper_online.py

# ---- Voice Activity Detection (VAD) ----
silero-vad>=5.1

# ---- Language Model (LLM) ----
openai>=2.0.0
# vLLM runs as a separate sidecar service (see docker-compose.yml)

# ---- Text-to-Speech (TTS) ----
edge-tts>=7.0.0
transformers>=4.52.1
huggingface_hub>=0.28.1
# CSM-1B loaded via transformers CsmForConditionalGeneration

# ---- Testing ----
pytest>=8.0.0
pytest-asyncio>=0.25.0
httpx>=0.27.0
